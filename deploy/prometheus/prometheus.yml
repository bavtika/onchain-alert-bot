global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: onchain-bot
    metrics_path: /metrics
    static_configs:
      - targets: ["bot:8080"]
        labels:
          service: onchain-alert-bot
