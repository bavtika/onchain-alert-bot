# Example only — do NOT commit real secrets.
# Apply with:
#   kubectl create secret generic onchain-secrets \
#     --from-literal=telegram-bot-token='YOUR_TOKEN' \
#     --from-literal=telegram-chat-id='YOUR_CHAT_ID'
#
# Or copy this file, fill values (base64), and apply once locally:
#   kubectl apply -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: onchain-secrets
type: Opaque
stringData:
  telegram-bot-token: "REPLACE_ME"
  telegram-chat-id: "REPLACE_ME"
