apiVersion: v1
kind: Service
metadata:
  name: onchain-alert-bot
  labels:
    app: onchain-alert-bot
spec:
  type: ClusterIP
  selector:
    app: onchain-alert-bot
  ports:
    - name: health
      port: 8080
      targetPort: health
      protocol: TCP
